#Let L be the language of ASCII strings that contain the character 'z'
#Write a python program that recognizes L, but does not decide L.

def recognizeInput(inputString):
    value = recognizeInput(inputString)
    for i in value:
        if(i == 'z')
            return 'yes'
        else:
            return 'no'
            
