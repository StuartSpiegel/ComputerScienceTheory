#Program takes two parameters (P, I) and outputs 'yes' if and only if
# P(I) = 'no'

def noOnStringApprox(inputString):
    value = noOnStringApprox(inputString)
    if (value == 'no')
        return 'yes'
    else:
        return 'no'
